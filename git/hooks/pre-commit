# This disables `git commit -am`.
#
# It removes from the working tree all the changes except the
# ones already added, filterint the ones added with `-a / --all`
# 
# In practice, this makes me separate the commits in two steps

# 1. `git add`
# 2. `git commit`

git stash save -q --keep-index "possible commit am"
